[tool.ruff]
line-length = 120

target-version = "py310"

exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__"
]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]

ignore = []

[tool.ruff.format]
indent-style = "space"